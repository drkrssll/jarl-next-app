/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.adaptivewfs.com',
      'utfs.io',
      'media2.giphy.com',
      'www.gifpng.com'
    ],
  },
};

export default nextConfig;
